# Supabase (REQUIRED — copy to .env.local, never commit real values)
# https://supabase.com/dashboard -> Project Settings -> API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Slack (optional — incoming webhook for posting game results)
VITE_SLACK_WEBHOOK_URL=